.build/
*.xcodeproj/
*.xcworkspace/
.swiftpm/
.DS_Store
.vscode

# Python analysis package (analysis/)
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Sim/analysis outputs (large; not source)
runs/

# Private, per-user site config — real Nightscout URLs/tokens/replay config.
# NEVER commit. Copy PRIVATE.example.md -> PRIVATE.md and keep it local.
PRIVATE.md

# Per-checkout role selection. Copy ROLE.example.md -> ROLE.md.
ROLE.md
